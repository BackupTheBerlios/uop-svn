entity start
	method constructor()
		var element p = process.new();
		{"context"}.bind("Eu");
		io.writeln("Resultado: ", {"context"}.data.get("Resultado"));
	end
end

entity process [parallel]
	method constructor()
		{"context"}.bind("Eu");
		{"context"}.data.publish("Resultado" => 10 * 2);
	end
end

