entity start
	def adicao(int x, int y) [int]
		return(x + y)
	end
	def subtracao(int x, int y) [int]
		return(x - y)
	end
	def start()
		int a, s
		a = adicao(10,20)
		s = subtracao(10,20)
		io.writeln("Adicao dos valores    : ", a)
		io.writeln("Subtracao dos valores : ", s)
	end
end

