entity start
	def start()
		io.writeln("Entidades existentes em MeuGrupo")
		foreach(element e in {"MeuGrupo"}.entitieslist)
			io.writeln("  Nome: " + e.name)
			{"MeuGrupo"}.data.af(e.name => "Bom dia " + e.name + " !!!")
		end
	end
end

