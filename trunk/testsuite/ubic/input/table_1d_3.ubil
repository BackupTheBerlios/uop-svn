entity start
	def start()
		def table tab
		def int index

		for(index=1; index<=10; index=index+1)
			tab[index] = index*10
		end

		for(index=1; index<=10; index=index+1)
			writeln("tab[", index, "]=", tab[index])
		end
	end
end

