Entity Start:
  def start():
    Process p = Process.new()
    writeln("Resultado: " +
      ["group"].data -= {"Resultado"})

Entity Process:
  def process():
    ["group"].data += {"Resultado", 10 * 2}

