import datetime;

entity start
	method constructor()
		var element insere = insert_data.new();
		{"MeuContexto"}.bind("start");
		io.writeln("Informacao inserida: ", {"MeuContexto"}.data.find("Numero 1"));
	end
end

entity insert_data [parallel]
	method constructor()
		{"MeuContexto"}.bind("insert");
		datetime.sleep(5);
		io.writeln("Inserindo informacao");
		{"MeuContexto"}.data.publish("Numero 1" => 100);
	end
end

